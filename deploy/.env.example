# Immutable image tag deployed by GitHub Actions.
APP_IMAGE=ghcr.io/ultistatsdev/ultistats-backend:COMMIT_SHA

# Generate a long random value. Never commit the real password.
POSTGRES_PASSWORD=replace-me
